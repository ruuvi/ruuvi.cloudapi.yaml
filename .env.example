# Copy this file to `.env` and fill in the real values.
#
# Tokens should include the `Bearer` prefix if required by the API, e.g.:
#   USER_TOKEN="Bearer eyJhbGciOi..."
#
# The script in `scripts/run-schemathesis.sh` can switch between these
# environments when executing Schemathesis tests.

# --- Test environment ----------------------------------------------------
TEST_BASE_URL=https://testnet.ruuvi.com
TEST_USER_TOKEN=Bearer test-token
TEST_SHAREE_ACCOUNT=test@example.com

# --- Production environment ---------------------------------------------
PROD_BASE_URL=https://network.ruuvi.com
PROD_USER_TOKEN=Bearer prod-token
PROD_SHAREE_ACCOUNT=prod@example.com